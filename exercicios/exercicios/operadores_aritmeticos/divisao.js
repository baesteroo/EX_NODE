// Crie duas variaveis, uma para a quantidade de pessoas e outra para a quantidade de bolinhos.

function opDivisao(pessoa, bolinhos){

    let divisor = bolinhos / pessoa
        return (divisor)

}

export {opDivisao}