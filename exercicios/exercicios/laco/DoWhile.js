/* DO WHILE
O progama vai contar de forma decrescente de 1 a 50 usando o Do While :) (Usado para executar uma coisa 1 vez)*/

function doWhile(numero){
    do {
        console.log(numero);
        numero--;
} 
    while (numero >= 1);
    return(resultado)


}

export {doWhile}
