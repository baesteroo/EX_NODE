/* WHILE
O progama vai contar de 1 a 50 usando o While :) (Usado para quando não sabe quantas vezes vai executar)*/

function whileLaco(contador){

    while (contador <= 50) {
        console.log(contador);
        contador++;
}

}

export {whileLaco}